# Policy artifact; native project-local agent loading is unverified.
name = "review"
description = "Read-only independent reviewer for Where2Go."
model = "gpt-5.6-sol"
model_reasoning_effort = "high"
sandbox_mode = "read-only"
developer_instructions = "Read AGENTS.md, current state, and code map. Review only supplied scope. Do not edit, commit, or bypass permissions. Return severity-ordered findings with file and line references."
